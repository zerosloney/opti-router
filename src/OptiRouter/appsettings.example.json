{
  "_readme": "模型配置已移至 models-config.json（Dashboard 可视化管理）。此处仅保留系统级配置。ApiKey 也可通过环境变量 OptiRouter__Models__0__ApiKey=sk-xxx 覆盖。",
  "OptiRouter": {
    "_comment_proxy_security": "入站 /v1/* Bearer 密钥与按来源 IP 的每分钟请求上限。ProxyApiKey 为空时拒绝所有 /v1/* 请求。",
    "ProxyApiKey": "",
    "_comment_admin_security": "管理端（/dashboard、/models、/api/*）独立密钥，仅 AdminApiKey 可登录管理台。为空时管理台鉴权失败。务必配置独立的 AdminApiKey，与代理 key 分离，降低权限提升风险。",
    "AdminApiKey": "",
    "_comment_admin_cookie_secure": "管理会话 Cookie 是否强制 HTTPS（Secure 标志）。默认 true：Cookie 仅经 HTTPS 下发，防中间人窃取。纯内网 HTTP 部署可设 false。",
    "AdminCookieRequireHttps": true,
    "RequestsPerMinute": 60,
    "MaxConcurrentRequestsPerPartition": 100,
    "TrustProxyHeaders": false,
    "_comment_models": "模型列表请在 Dashboard UI 中配置（/dashboard?key=xxx），或直接编辑 models-config.json。此处 Models 必须为空数组。每个模型可选 Id（上游真实模型名，如 \"deepseek-chat\"；留空回退 Name）、Name（路由名；只配置 Id 且留空 Name 时自动生成为「供应商/Id」，同供应商同模型多 Key 冲突时追加 #2/#3）、Provider/Family（自由字符串，用于 Fusion 多样性与供应商标识显示）、CachedInputPricePerMillion/CacheWriteInputPricePerMillion（null 时回退普通输入价格）。",
    "Models": [],
    "_comment_budget": "日预算与预算耗尽行为。UsePersistentStore=true 时成本账本落 SQLite，跨重启保留；false 用内存（重启归零）。",
    "Budget": {
      "DailyBudgetUsd": 10.0,
      "SessionBudgetUsd": null,
      "EnforceOnExhausted": "Degrade",
      "UsePersistentStore": true,
      "StorePath": "data/optirouter-budget.db",
      "SessionEvictionHours": 24
    },
    "_comment_routing": "路由策略四开关及通用参数。",
    "Routing": {
      "EnableRuleClassifier": true,
      "EnableTokenEstimator": true,
      "EnableBudgetGuard": true,
      "EnableFailover": true,
      "LongInputThresholdTokens": 32000,
      "DefaultTier": "Medium",
      "TokenEstimation": "Tiktoken",
      "TiktokenEncoding": "o200k_base",
      "FailoverFailureThreshold": 3,
      "FailoverCooldownSeconds": 60,
      "FailoverGlobalTimeoutSeconds": 0,
      "_comment_stream_ttft": "流式首字节(TTFT)专项超时(毫秒)。0=禁用(退回整体超时)；>0 时首字节未到此时间即 failover 到下一候选，避免干等慢上游。仅流式。",
      "StreamFirstTokenTimeoutMs": 0,
      "_comment_response_cache": "响应缓存(仅非流式)。按规范化请求哈希精确缓存，命中短路返回(零成本零延迟)。适合幂等请求(分类/提取/翻译)。键在PII脱敏前计算，不会因占位符相同串扰。",
      "EnableResponseCache": false,
      "ResponseCacheTtlSeconds": 3600,
      "ResponseCacheMaxEntries": 1000,
      "_comment_cost_aware": "成本感知reward权重α。0=禁用(默认,reward仅看延迟/质量)；>0时reward=(1-α)·原reward+α·costReward,costReward按token归一化(cost×1e6/tokens,消除长输入=贵模型偏差),引导偏好真正便宜的模型。建议0.2-0.4。",
      "CostAwareWeight": 0.0,
      "CostAwareBaselineUsd": 1.0,
      "_comment_quality_penalty": "质量惩罚因子∈[0,1]。主路径成功请求若低质量(finish_reason=length截断/content_filter/空content/JSON契约违约),reward按此因子乘性折减(finalReward=latencyReward×qualityFactor)。默认0.3(打三折);1.0=不惩罚。",
      "QualityPenaltyFactor": 0.3,
      "_comment_regenerate": "regenerate负反馈:同一规范化请求在窗口内重发且上次成功→视为用户对上次答案不满意,给上次模型注入低reward。零额外调用的质量信号;对同一请求的例行重复调用(定时任务固定prompt)会误判,有此类流量保持关闭。",
      "EnableRegenerateFeedback": true,
      "RegeneratePenaltyReward": 0.1,
      "RegenerateFeedbackWindowSeconds": 600,
      "_comment_failover_halfopen": "熔断冷却到期后进入半开态，最多放行这么多并发探测请求；探测成功闭合熔断，失败重新冷却。",
      "FailoverHalfOpenMaxProbes": 1,
      "FailoverHalfOpenRequiredSuccesses": 1,
      "_comment_health_probe": "后台主动探活。启动预热一轮后按间隔周期探测所有启用模型，结果上报断路器。",
      "EnableHealthProbe": true,
      "HealthProbeIntervalSeconds": 60,
      "_comment_semantic": "离线路由：Hybrid 为 TF-IDF 高置信短路 + 第二阶段；内置 Dense 是稳定词法特征哈希，不是训练 embedding。",
      "EnableSemanticRouter": true,
      "SemanticRouterMode": "Hybrid",
      "HybridHighConfidenceThreshold": 0.45,
      "SemanticSimilarityThreshold": 0.25,
      "EnableSessionAffinity": false,
      "SessionAffinityTtlSeconds": 600,
      "_comment_prompt_cache_affinity": "稳定前缀缓存粘性：仅保存 system/tool/schema 稳定材料的 SHA-256，不保存原始 prompt；按 TTL 软提升上次成功模型。默认关。",
      "EnablePromptCacheAffinity": false,
      "PromptCacheAffinityTtlSeconds": 600,
      "_comment_quota_aware": "配额感知：只读进程内已规范化的上游 rate-limit 快照，软降级低余量并在已知 reset 窗口内排除耗尽模型。默认关，重启后快照丢失。",
      "EnableQuotaAwareRouting": false,
      "EnableLoadBalance": false,
      "EnableCascadeUpgrade": false,
      "CascadeUpgradeSampleRate": 0.1,
      "CascadeUpgradeSelfVerifyPrompt": "",
      "_comment_cascade_verifier": "级联校验模型名(他评,消除自评偏差)。留空=自评(用Cheap模型校验自己);配置时按名匹配已启用模型,找不到回退自评。建议填Strong模型做校验。",
      "CascadeUpgradeVerifierModel": null,
      "_comment_latency_aware": "延迟感知：后台聚合模型延迟，同 tier 段按延迟重排（快模型优先）。默认关。LatencyMinSamples 为生效最小样本数，LatencyStatsWindowMinutes 为聚合窗口。",
      "EnableLatencyAware": false,
      "LatencyMinSamples": 10,
      "LatencyStatsWindowMinutes": 60,
      "_comment_thompson": "Thompson Sampling 自适应路由（段内 Beta 分布采样重排）。与 EnableContextualBandit 互斥，不能同时开启。",
      "EnableThompsonSampling": false,
      "ThompsonDiscountFactor": 0.95,
      "ThompsonLatencyTargetMs": 800.0,
      "_comment_thompson_per_tier": "Per-tier延迟目标(ms)。命中且>0覆盖ThompsonLatencyTargetMs;未配置tier回退全局。消除全局单target系统性偏Cheap(强模型天生慢)。默认Strong:1500/Medium:1000/Cheap:600。",
      "ThompsonLatencyTargetMsByTier": {
        "Strong": 1500.0,
        "Medium": 1000.0,
        "Cheap": 600.0
      },
      "ThompsonRaceCancelledReward": 0.5,
      "_comment_exploration": "ε探索保底∈[0,1]。>0时段内重排后以概率ε把随机非首位模型提到段首,保证尾部模型持续获得流量样本,修低流量下'尾部锁死'反馈环。自用低流量建议0.05;0=纯贪心。",
      "ExplorationEpsilon": 0.05,
      "_comment_contextual_bandit": "上下文老虎机（LinUCB）路由：带请求特征的打分重排。与 EnableThompsonSampling 互斥，不能同时开启。",
      "EnableContextualBandit": false,
      "ContextualBanditAlpha": 1.0,
      "ContextualBanditDiscountFactor": 0.95,
      "_comment_capability_filter": "能力过滤：按请求能力需求（vision/tool-use/json-mode）排除 Tags 不含的模型。需在 models-config.json 为模型配置 Tags 字段。",
      "EnableCapabilityFilter": false,
      "_comment_fusion": "并行首试：非流式首轮并行前 N 候选取最快成功，取消其余。被取消/失败的尝试按估算 input 记预估成本（IsEstimated=true）。流式不支持。",
      "EnableFusionMode": false,
      "FusionMaxParallel": 2,
      "_comment_fusion_hedge": "Hedged request 延迟(毫秒)。0=总是并行(默认，N×成本)；>0 时主候选(admitted[0])立即启动，其余延迟此时间后才启动，期间主成功则不发(1×成本)。仅非流式。",
      "FusionHedgeDelayMs": 0,
      "_comment_fusion_router": "质量融合路由 panel→analyst→outer。动态 panel 与 provider/family 多样性均默认关闭；FusionRouterPanelSize 同时作为动态最大值。",
      "EnableFusionRouter": false,
      "FusionRouterPanelSize": 3,
      "EnableDynamicFusionPanelSize": false,
      "FusionRouterMinPanelSize": 2,
      "EnableFusionDiversity": false,
      "_comment_fusion_panel_timeout": "Panel 级超时秒数。0=不启用（向后兼容，仅靠全局 ct 兜底）；>0 时每个 panel 有独立 CTS，超时计 408 失败、记断路器、排除 analyst/outer 回退候选。建议 30-120。",
      "FusionRouterPanelTimeoutSeconds": 0,
      "FusionRouterAnalystModel": null,
      "FusionRouterAnalystPrompt": null,
      "FusionRouterOuterModel": null,
      "FusionRouterMaxOutputTokens": 16000,
      "FusionRouterTemperature": 0.0,
      "FusionRouterPanelTemperature": null,
      "FusionRouterMinComplexity": "Unknown",
      "SemanticRoutes": [
        {
          "Name": "code-assistance",
          "TargetTier": "Strong",
          "Phrases": [
            "write a python function to compute fibonacci",
            "implement binary search in rust",
            "compile build fix runtime exception stacktrace"
          ]
        },
        {
          "Name": "casual-chit-chat",
          "TargetTier": "Cheap",
          "Phrases": [
            "how is the weather today",
            "tell me a funny story",
            "hello who are you"
          ]
        }
      ],
      "_comment_max_stream_bytes": "流式响应累计字节硬上限，防 OOM/恶意无限流输出。默认 20MB。",
      "MaxResponseStreamBytes": 20971520
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
