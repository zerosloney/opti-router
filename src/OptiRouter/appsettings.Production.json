{
  // 部署环境（默认 Production）启用 MariaDB 后端：配置库 + 成本账本 + 审计 + 学习状态全部落 MariaDB。
  // 放在环境分层文件而非 appsettings.json，避免集成测试复制主配置启动时误连真实库。
  // 本地开发（Development，dotnet run）仍走 SQLite 文件后端。
  "OptiRouter": {
    "ConfigDbConnectionString": "Server=127.0.0.1;Port=3306;Database=test;User ID=root;Password=dc123456;Connection Timeout=15;Default Command Timeout=30",
    "Budget": {
      "StoreProvider": "MariaDb",
      "MariaDbConnectionString": "Server=127.0.0.1;Port=3306;Database=test;User ID=root;Password=dc123456;Connection Timeout=15;Default Command Timeout=30"
    }
  }
}
